{
  "name": "il-est-ecrit",
  "private": true,
  "scripts": {
    "build": "node scripts/build-portable.mjs",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs",
    "db:generate": "drizzle-kit generate",
    "start": "node --env-file-if-exists=.env portable/node.mjs",
    "db:migrate": "node --env-file-if-exists=.env scripts/migrate-postgres.mjs",
    "build:legacy-sites": "node scripts/build.mjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2",
    "esbuild": "0.25.12"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "pg": "^8.23.0"
  }
}
